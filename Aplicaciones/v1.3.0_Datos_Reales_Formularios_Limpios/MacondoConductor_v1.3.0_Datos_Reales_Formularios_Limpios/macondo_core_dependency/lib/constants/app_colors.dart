import 'package:flutter/material.dart';

class AppColors {
  // Primarios de Marca Macondo Express
  static const Color primary = Color(0xFF0F172A);      // Slate 900
  static const Color primaryLight = Color(0xFF1E293B); // Slate 800
  static const Color accent = Color(0xFF10B981);        // Emerald Express
  static const Color accentLight = Color(0xFF34D399);   // Emerald Glow
  static const Color amber = Color(0xFFF59E0B);         // Warm Gold / Alertas
  
  // Fondos y Superficies
  static const Color background = Color(0xFF090D16);   // Dark Cosmos
  static const Color surface = Color(0xFF131B2A);      // Dark Card Surface
  static const Color surfaceElevated = Color(0xFF1C273C);
  
  // Estados de Asientos
  static const Color seatAvailable = Color(0xFF10B981); // Verde libre
  static const Color seatSelected = Color(0xFF00F0FF);  // Cyan seleccionado
  static const Color seatOccupied = Color(0xFF475569);  // Gris ocupado
  
  // Textos
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textDim = Color(0xFF64748B);
  
  // Bordes y Divisores
  static const Color border = Color(0xFF243046);
  static const Color divider = Color(0xFF1E293B);
}
